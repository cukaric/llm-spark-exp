# Models

Keep lightweight metadata, adapters, or test fixtures here when they are small enough for git.
Large checkpoints should stay outside git, with download or training instructions committed instead.
