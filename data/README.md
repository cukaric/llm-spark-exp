# Data

Keep small, non-sensitive sample data here only when it is useful for tests or examples.

- `raw/`: immutable source files
- `processed/`: derived datasets
- `external/`: third-party reference data

Large datasets should live outside git and be pulled through a reproducible script or documented
download step.
