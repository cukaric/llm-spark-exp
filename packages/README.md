# Packages

This is reserved for future first-party packages if the repo grows into a workspace.

Keep the project as a single package under `src/llm_spark_exp` until separate packages need distinct
dependencies, release cadence, or Docker build contexts.
